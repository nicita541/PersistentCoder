annotated-types==0.8.0
colorama==0.4.6
iniconfig==2.3.0
markdown-it-py==4.2.0
mdurl==0.1.2
packaging==26.3
pluggy==1.6.0
pydantic==2.13.5
pydantic_core==2.46.5
Pygments==2.21.0
pytest==9.1.1
rich==15.0.0
typing-inspection==0.4.4
typing_extensions==4.16.0

# ------------------------------------------------------------------
# Runtime ML dependencies (used by app/llm/client.py).
# torch is intentionally unpinned and WITHOUT a CUDA index URL so this
# requirements file stays portable between CPU-only and CUDA machines.
#   CPU:   pip install torch
#   CUDA:  pip install torch --index-url https://download.pytorch.org/whl/cu128
# ------------------------------------------------------------------
torch
transformers
accelerate
huggingface_hub
safetensors
